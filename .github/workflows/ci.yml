name: Nix Checker
on:
  push:
    branches: [main]
  pull_request:
    branches: [main, dev]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Installs current Nix rather than a version frozen at the action's
      # release date. install-nix-action pinned Nix 2.19.1, which rejects store
      # path names beginning with a dot and so could not evaluate this flake.
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          # Kept identical to build.yml. flake.nix's nixConfig lists four more
          # substituters, but Nix discards them here -- "ignoring untrusted
          # flake configuration setting" -- as the runner is not a trusted user.
          # Declaring them at this level makes them trusted instead. The fastly
          # entry is a CDN mirror of cache.nixos.org; the flake's USTC mirror is
          # deliberately left out, since it serves Asia and these runners are
          # not there.
          extra-conf: |
            substituters = https://aseipp-nix-cache.global.ssl.fastly.net https://cache.nixos.org/ https://nix-community.cachix.org https://cache.numtide.com
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs= niks3.numtide.com-1:DTx8wZduET09hRmMtKdQDxNNthLQETkc/yaX7M4qK0g=
      - name: Setup Cachix
        uses: cachix/cachix-action@v17
        with:
          name: lemonilemon
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
      - name: Run checks
        run: nix flake check
      - name: Check formatting
        run: |
          nix fmt
          if ! git diff --quiet; then
            echo "❌ Code is not formatted properly"
            echo "Run 'nix fmt' locally and commit the changes"
            git diff
            exit 1
          fi
      - name: Dry build NixOS-wsl configuration
        run: |
          echo "🔍 Dry building NixOS-wsl configuration..."
          nix build .#nixosConfigurations.NixOS-wsl.config.system.build.toplevel --dry-run
      - name: Dry build laptop configuration
        run: |
          echo "🔍 Dry building laptop configuration..."
          nix build .#nixosConfigurations.laptop.config.system.build.toplevel --dry-run
      - name: Dry build desktop configuration
        run: |
          echo "🔍 Dry building desktop configuration..."
          nix build .#nixosConfigurations.desktop.config.system.build.toplevel --dry-run
